spring:
  application:
    name: userserivce
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      server-addr: localhost:8181 # nacos地址
      config:
        file-extension: yaml # 文件后缀名
